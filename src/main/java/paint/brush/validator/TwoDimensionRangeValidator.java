package paint.brush.validator;

import paint.Range;

public class TwoDimensionRangeValidator extends AbstractDimensionRangeValidator implements RangeValidator {

    public TwoDimensionRangeValidator() {
        super();
    }

    @Override
    public boolean validate(Range range) {
        return super.validate(range);
    }
}
